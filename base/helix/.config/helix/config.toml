# config.toml for Helix with Catppuccin Mocha theme

theme = "catppuccin_mocha"

[editor]
line-number = "relative"
mouse = true
cursorline = true
cursorcolumn = false
gutters = ["diagnostics", "line-numbers", "spacer", "diff"]
auto-completion = true
auto-format = true
auto-pairs = true
auto-save = false
idle-timeout = 50
completion-trigger-len = 2
rulers = []
color-modes = true

[editor.cursor-shape]
insert = "bar"
normal = "block"
select = "underline"

[editor.indent-guides]
render = true
character = "│"
skip-levels = 1

[editor.statusline]
left = ["mode", "spinner"]
center = ["file-name"]
right = ["diagnostics", "selections", "position", "file-encoding", "file-line-ending", "file-type"]
separator = "│"
mode.normal = "NORMAL"
mode.insert = "INSERT"
mode.select = "SELECT"

[editor.lsp]
display-messages = true

[editor.soft-wrap]
enable = false

[keys.normal]
esc = ["collapse_selection", "keep_primary_selection"]
G = "goto_file_end"
"ret" = ["move_line_down", "goto_first_nonwhitespace"]
"C-s" = ":w"
"C-q" = ":q"
"C-c" = ["collapse_selection", "keep_primary_selection", "normal_mode"]
"C-f" = "page_down"
"C-b" = "page_up"
0 = "goto_line_start"
"$" = "goto_line_end"
"^" = "goto_first_nonwhitespace"
"A-j" = ["extend_to_line_bounds", "delete_selection", "paste_after"]
"A-k" = ["extend_to_line_bounds", "delete_selection", "move_line_up", "paste_before"]

[keys.insert]
"C-c" = ["collapse_selection", "normal_mode"]
"C-s" = [":w", "normal_mode"]

# Create ~/.config/helix/themes/catppuccin_mocha.toml with the following content:
# or clone it from https://github.com/catppuccin/helix

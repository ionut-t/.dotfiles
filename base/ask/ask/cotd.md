Suggest one useful command or tool for a macOS terminal.

Rules:

- Not basics like ls, cd, grep.
- Show the command, then explain in 1-2 short sentences. No walkthroughs.
- Flags of common tools count (e.g. `git log --oneline --graph`).
- Vary across: file management, networking, text processing, git, system info, productivity.
- Keep it to 3-4 lines max.
